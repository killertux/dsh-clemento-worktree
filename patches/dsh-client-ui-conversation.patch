diff --git a/lib/client.js b/lib/client.js
index 86e7906..d5fb86c 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -6783,7 +6783,7 @@ window.__ModuleLoader__.load({
 		};
 		//#endregion
 		//#region lib/types/client/skeleton/ConversationRoot.js
-		function ConversationRoot({ sessionId, useSession, useSessions, useWorkspaces, useInput, useComposerBlock, renderSlot, renderSlotChain, selectWorkspace, t }) {
+		function ConversationRoot({ sessionId, useSession, useSessions, useWorkspaces, useInput, useComposerBlock, renderSlot, renderSlotChain, selectWorkspace, worktreeWorkspaceOf, t }) {
 			const openState = useSession((s) => s.openState);
 			const composerPhase = useSession((s) => s.composerPhase);
 			const pending = useSession((s) => s.pending) ?? [];
@@ -6807,7 +6807,7 @@ window.__ModuleLoader__.load({
 				});
 				seatObserver.current.observe(seat);
 			}, []);
-			const sessionWorkspace = sessionId === void 0 ? void 0 : workspaces.items.find((workspace) => workspace.sessionIds.includes(sessionId));
+			const sessionWorkspace = sessionId === void 0 ? void 0 : workspaces.items.find((workspace) => workspace.sessionIds.includes(sessionId)) ?? workspaces.items.find((workspace) => workspace.workspaceId === worktreeWorkspaceOf?.(sessionId));
 			const pendingWorkspace = workspaces.items.find((workspace) => workspace.workspaceId === pendingWorkspaceId);
 			(0, react.useEffect)(() => {
 				if (pendingWorkspaceId === void 0) return;
@@ -6852,6 +6852,7 @@ window.__ModuleLoader__.load({
 							setPickerOpen(false);
 						}
 					}),
+					renderSlot("conversation.hero.actions", {}),
 					renderSlot("conversation.hero.agentPreset", {})
 				]
 			});
@@ -9559,10 +9560,15 @@ window.__ModuleLoader__.load({
 					"conversation.hero.agentPreset": {
 						kind: "single",
 						scope: "root"
+					},
+					"conversation.hero.actions": {
+						kind: "list",
+						scope: "root"
 					}
 				},
 				inject: (sessionId) => ({
 					hooks: { composerBlock: sessionId === void 0 ? ABSENT_BLOCK : composerBlocks.storeFor(sessionId) },
+					worktreeWorkspaceOf: (id) => { const m = ctx.get("worktreeWorkspace"); return m == null ? void 0 : m.workspaceOf(id) },
 					selectWorkspace: async (workspaceId) => {
 						const nextId = await workspaces.connectWorkspace(workspaceId);
 						if (sessionId !== void 0 && nextId !== sessionId) {
